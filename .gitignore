__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
coverage.xml
htmlcov/
dist/
build/
.venv/
.wheel-smoke/
.env
.env.*
!.env.example
outputs/*
!outputs/.gitkeep
*.tmp
.DS_Store

# Agent runtime state written by the governance L4 / PR flow. Session-local:
# phase and release receipts, PR remediation handoff. Never part of delivery.
.l9/autonomy/
.l9/pr/

# Agent adapter wiring written into the worktree by the governance sessionStart
# bootstrap: Claude Code settings, hooks, and the managed-skill index, plus the
# MCP server map. Session-local, machine-specific, and never part of delivery.
.claude/
.mcp.json
