[project]
name = "l9-mcp-server"
version = "0.2.0"
requires-python = ">=3.11"
dependencies = [
    "l9-gate-sdk>=0.1.0",
    "fastapi>=0.100",
    "uvicorn>=0.23",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"
